server:
  port: 80
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    password: root123
    username: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
